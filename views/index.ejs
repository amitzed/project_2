<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>String Theory</title>
        <!-- FONTS -->
        <link href="https://fonts.googleapis.com/css?family=Montserrat:300,400,700" rel="stylesheet">
        <!-- CSS -->
        <link rel="stylesheet" href="css/style.css">
    </head>
    <body>
      <h1>
          <% if(currentUser) { %>
              Welcome to String Theory <%=currentUser.username %>
          <% } else { %>
              Welcome to String Theory!
          <% }%>
      </h1>
      <nav>
          <ul>
              <li>
                  <a href="/sessions/new">Member Log In</a>
              </li>
              <li>
                  <a href="/users/new">Create Account</a>
              </li>
             <!-- ONCE User Logged In as a Current User: -->
              <% if(currentUser) { %>
                <br/>


              <% } %>
          </ul>
      </nav>
      <% if(currentUser) { %>
          <main>
              Click Here to  <a href="/guitars">Build Your Dream Guitars</a>
              <br/><br/>
              Click Here to  <a href="/basses">Build Your Dream Basses</a>
          </main>

      <% } %>

      <ul>
        <form action="/sessions?_method=DELETE" method="POST">
        <input type="submit" value="Logout"/>
        </form>
      </ul>
    </body>
</html>
