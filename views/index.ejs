<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Guitars Index Page</h1>
    <ul>
      <% for(let i = 0; i < guitars.length;
      i++){ %>
        <li>
          Your
            <a href="/guitars/<%=guitars[i].id%>">
              <%=guitars[i].brand; %>
            </a>
          with <%=guitars[i].strings; %>
          and made out of <%=guitars[i].wood; %>
          with a fret board made from <%=guitars[i].fretboard; %>.
          <% if(guitars[i].readyToBuild === true){ %>
            Your Guitar is Ready to Build
          <% } else { %>
            Rebuild Your Guitar
          <% } %>
          <form
          action="/guitars/<%=guitars[i].id%>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE"/>
          </form>
          <a href="/guitars/<%=guitars[i].id%>/edit">
            Edit
          </a>
        </li>
      <% } %>
    </ul>
    <nav>
      <a href="/guitars/new">Create a New Guitar</a>
    </nav>
  </body>
</html>
