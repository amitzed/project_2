<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>String Theory</title>
  </head>
  <body>
    <h1>Bass Guitars Index Page</h1>
    <ul>
      <% for(let i = 0; i < basses.length; i++){ %>
        <li>
          Your
            <a href="/basses/<%=basses[i].id%>">
              <%=basses[i].brand; %>
            </a>
          Bass Guitar with <%=basses[i].strings; %>
          strings which is made out of <%=basses[i].wood; %>
          wood with a <%=basses[i].fretboard; %> fretboard:
          <% if(basses[i].readyToBuild === true){ %>
            IS READY TO BE BUILT!
          <% } else { %>
            CANNOT be built to these specs
          <% } %>
          <form
          action="/basses/<%=basses[i].id%>?_method=DELETE" method="POST">
          <br/>
            <input type="submit" value="DELETE"/>
          </form>
          <br/>
          <a href="/basses/<%=basses[i].id%>/edit">
            Edit
          </a>
        </li>
      <% } %>
    </ul>
    <nav>
      <a href="/basses/new">Create a New Bass Guitar</a>
    </nav>

    <br/>
    <nav>
      <a href="/">Return to Main Page</a>
    </nav>

    <br/>
    <form action="/sessions?_method=DELETE" method="POST">
        <input type="submit" value="Logout"/>
    </form>
  </body>
</html>
